{% block navigation %}
 <H1>查詢</H1>
{{ boards }}
{% for board in boards %}
    <article>

<form action="{{ path('BoardBundle_modify') }}" name="mod" method="post">

  <table border="1">
      <tr>
          <td> <h3>留言人:{{ boards.user }}</h3></td>
          <td><h4>{{ users.updateTime|date('m-d H:i:s') }}</h4></td>
      </tr>
      <tr>
           <td colspan="2" > <h3>標題:{{ users.title }} </h3></td>
      </tr>
      <tr>
           <td colspan="2"  width="200px" > <p>內容:</br>{{ users.message }}</p></td>
      </tr>
      <td>
            <input type="hidden" name="id" value="{{ users.id }}">
            <input type="submit" name="action" value="刪除">
            <input type="submit" name="action" value="修改">
      </td>
  </table>

</form>

    </article>
 {% else %}
        <p>There are no message </p>
    {% endfor %}
 
    {% endblock %}